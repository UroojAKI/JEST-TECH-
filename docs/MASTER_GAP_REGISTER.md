# MASTER GAP REGISTER — JEST POLICY CRM
# Version: 1.0.0 | Branch: production-remediation | Date: 2026-08-27
# Status: ACTIVE — updated per release as gaps are closed
#
# Severity: P0 = release blocker | P1 = major gap | P2 = significant | P3 = minor

---

| ID   | Sev | Release | Module         | Problem                                                  | Current Behavior                                                   | Expected Behavior                                                                              | Root Cause                                                              | Required Change                                                                   | Status      |
|:-----|:----|:--------|:---------------|:---------------------------------------------------------|:-------------------------------------------------------------------|:-----------------------------------------------------------------------------------------------|:------------------------------------------------------------------------|:----------------------------------------------------------------------------------|:------------|
| G001 | P0  | R1      | Auth           | Login does not route to role-based dashboard             | Login appears to redirect directly to Motor form or workspace      | Login → ActorContext → resolve landingWorkspace → /workspace/{role}                            | Auth service does not return workspaces[]; frontend hardcodes redirect  | Auth service: return workspaces[]+landingWorkspace; frontend: role-based routing  | CLOSED      |
| G002 | P0  | R1      | Auth           | Workspace switcher not permission-driven                 | All workspaces likely shown regardless of role                     | Only authorized workspaces visible                                                             | workspace list hardcoded in useWorkspace hook or navigation config      | Derive workspace list from actor.permissions at API layer                         | CLOSED      |
| G003 | P0  | R2      | Authorization  | Agent A can see Agent B's records (BOLA risk)            | List endpoints may not filter by assignedToId                      | Agent A sees only own assigned records; Agent B's records → 404                                | Query scoping not applied; ScopeResolverService not wired to repositories| Wire ScopeResolverService to every list endpoint; test 15 BOLA scenarios          | CLOSED      |
| G004 | P0  | R3      | Lead           | Motor form asks user to re-enter lead data               | Motor form is blank; customer name/phone/vehicle not prefilled      | GET /leads/:id/context → prefill all captured fields; locked UI                               | No Lead Context API; Motor form has no prefill source                   | Build GET /leads/:id/context; Motor wizard reads and prefills from it             | CLOSED      |
| G005 | P0  | R3      | Lead           | No duplicate detection on lead creation                  | Creating lead with duplicate phone/email creates new identity silently| Show "Existing customer found" with merge/use-existing options                                | No deduplication service on lead create                                 | DeduplicationService: check phone, PAN, registrationNumber before create          | CLOSED      |
| G006 | P0  | R5      | Quotation      | No QuotationGroup/QuotationVersion model                 | Each calculation creates an independent Quotation row              | QuotationGroup (V1, V2, V3...) linked to lead; one ACCEPTED per group                         | Schema lacks QuotationGroup entity; versions not tracked                | Prisma migration: QuotationGroup + QuotationVersion; immutable snapshot per version| OPEN       |
| G007 | P0  | R6      | Inspection     | inspectionRequired=true does NOT create inspection record | workflow sets workflowState='INSPECTION_REQUIRED' but no Inspection row created (confirmed at motor-quote-workflow.service.ts:151) | Rule engine → atomically create Inspection + Task in same transaction  | MotorQuoteWorkflowService does not create Inspection on rule evaluation | Add atomic Inspection + Task creation in evaluatePreviousPolicy transaction        | OPEN        |
| G008 | P0  | R7      | Payment        | Payment amount not server-validated against quote        | Client submits any amount; server does not compare to payableAmount | reconciledAmount MUST == quotation.totalPayable; underpayment → 422                            | payableAmount not fetched and compared during reconciliation            | PaymentReconciliationService: fetch acceptedVersion.totalPayable; enforce equality| OPEN        |
| G009 | P0  | R8      | Issuance       | No single issuance authority / no IssuanceEligibilityGate| Multiple paths can create policies (direct POST /policies, quotation convert, motor issue) | One PolicyIssuanceService with Gate A check; all other paths removed   | No canonical gate; multiple issuance entry points exist                 | Consolidate into single PolicyIssuanceService; implement Gate A        | OPEN        |
| G010 | P0  | R9      | Policy         | IssuanceEligibilityGate requires insurer data too early  | Spec contradiction: insurerPolicyNumber/documentKey required for READY_FOR_ISSUANCE | Split into Gate A (READY_FOR_ISSUANCE, no insurer data) and Gate B (ISSUED, insurer data required) | Gate spec not correctly split into two phases                           | See contract 02-state-machines.md §9; implement two-phase gate         | IN_CONTRACT |
| G011 | P0  | R9      | Policy         | Policy expiry not from authoritative InsurerPolicyDetail  | odExpiryDate/tpExpiryDate may be optional or taken from quotation  | All expiry dates mandatory from InsurerPolicyDetail before Policy→ACTIVE | InsurerPolicyDetail entity does not exist; dates not enforced            | Prisma migration: InsurerPolicyDetail; enforce before ACTIVE transition | OPEN        |
| G012 | P0  | R10     | Renewal        | Renewal uses non-atomic findFirst→create (duplicate risk) | scheduler creates renewal tasks without unique constraint check    | upsert with UNIQUE(policyId, offsetDays); distributed Redis lock        | renewal-scheduler.cron.ts uses non-idempotent pattern                  | Refactor to upsert + Redis SETNX lock; add DB unique constraint        | OPEN        |
| G013 | P0  | R10     | Renewal        | renewalDueDate not from InsurerPolicyDetail               | renewalDueDate derived from quotation validity or not present      | MIN(odExpiryDate, tpExpiryDate) from InsurerPolicyDetail only          | G011 (InsurerPolicyDetail does not exist)                               | Fix after G011; wire renewal scheduler to InsurerPolicyDetail          | BLOCKED_G011|
| G014 | P0  | R12     | Customer 360   | Customer 360 returns hardcoded/mock data                 | Tabs show static arrays (Sunita Mehta, POL-001 etc)               | Every tab queries database: leads, policies, claims, payments, renewals | customer-360.service.ts has inline static data                         | Replace all static arrays with Prisma queries scoped to customerId     | OPEN        |
| G015 | P0  | R12     | Dashboards     | KPI cards are not clickable / not real data              | Numbers shown but no drill-down; may be hardcoded counts           | Every KPI links to filtered API endpoint returning real records         | KPI cards are display-only; no href; data from hardcoded source        | Add href to every KpiCard; compute from SQL aggregation                | OPEN        |
| G016 | P1  | R1      | Auth           | INSPECTOR role does not exist in canonical role list     | Contract v1.0 lacked INSPECTOR; role undefined                    | INSPECTOR is a canonical role with explicit permissions (see contract 01) | Contract omission                                                       | Add INSPECTOR role to Prisma enum + ROLE_SCOPE_MATRIX contract ✓       | IN_CONTRACT |
| G017 | P1  | R6      | Documents      | Document lifecycle not server-persisted (upload ≠ verify) | Uploading a file may mark it as "done" without verification step  | UPLOADED → UNDER_REVIEW → VERIFIED; separate actor required for VERIFIED | Document state machine not enforced                                     | Implement DocumentVerificationService; enforce state machine           | OPEN        |
| G018 | P1  | R5      | Quotation      | Addon with zero premium not blocked (silent defect)      | An addon can be selected and return premium=0 without error        | Selected addon with zero premium → calculation error (not silent zero) | MotorPricingService allows zero-premium addons                         | Add validation: eligibleAddon.premium > 0 OR explicit ineligibilityReason| OPEN      |
| G019 | P1  | R4      | Lead           | Lead has no completion score / stage-gate enforcement    | Lead can proceed to quotation with incomplete vehicle data          | completionScore shown; stage gates block progression                   | No LeadCompletionService                                               | Build LeadCompletionService with 5-stage model                         | CLOSED      |
| G020 | P1  | R7      | Finance        | No Finance workspace queue                               | Finance has no dedicated reconciliation queue                      | Finance Executive sees pending reconciliation items sorted by urgency  | Finance workspace exists but shows generic data                        | Build GET /finance/reconciliation-queue; Finance workspace query        | OPEN        |
| G021 | P1  | R8      | Back Office    | No Back Office workbench queue                           | Operations workspace shows generic data, no actionable issuance cases| Queue: Customer + Agent + Quote + Payment + Documents + Inspection + SLA + NextAction | No GET /back-office/queue endpoint with gate status    | Build GET /back-office/queue; join all gate data                       | OPEN        |
| G022 | P1  | R13     | Audit          | AuditLog not transactional (can be missed on error)      | audit events may be logged separately from business mutation       | AuditLog in same DB transaction as business mutation                   | Audit calls happen after await, outside transaction                    | Wrap audit inside prisma.$transaction with business mutations          | OPEN        |
| G023 | P1  | R13     | Audit          | AuditLog not immutable (no DB-level constraint)          | No PostgreSQL policy preventing UPDATE/DELETE on audit_logs        | Database policy: audit_logs is append-only                             | No DB-level immutability enforcement                                   | Add Prisma/PostgreSQL row-level policy on audit_logs table             | OPEN        |
| G024 | P2  | R2      | Multi-user     | Team/Branch scope not wired to list queries              | ScopeResolverService exists but not wired to repositories          | Every list endpoint applies organizationId+branchId+teamId filter      | ScopeResolver not called in repository layer                           | Wire ScopeResolver to all repository findMany() calls                  | OPEN        |
| G025 | P2  | R3      | Customer       | No canonical Customer entity distinct from Lead/Contact  | Customer/Contact/Lead may not have clear deduplication key         | Customer: unique by PAN or phone; Lead links to Customer               | Schema may conflate Contact and Customer                               | Verify Prisma schema; add deduplication unique constraints             | OPEN        |
| G026 | P2  | R9      | Policy         | Policy activation not automatic (startDate <= today)     | Policy must be manually activated or does not auto-activate        | When startDate <= today AND status=ISSUED → scheduler sets status=ACTIVE | No scheduler for ISSUED→ACTIVE transition                              | Add scheduled job: ISSUED policies with startDate <= today → ACTIVE   | OPEN        |
| G027 | P2  | R13     | Outbox         | No transactional outbox (async handoffs unreliable)       | Events may be published after DB commit (fire-and-forget)          | OutboxEvent in same transaction; outbox worker delivers eventually     | No outbox pattern implemented                                          | Implement OutboxEvent table + outbox worker                            | OPEN        |
| G028 | P2  | R14     | Auth           | No optimistic locking on mutable aggregates              | Concurrent edits may cause last-write-wins data corruption          | version field on all mutable aggregates; 409 on version mismatch      | version field may not exist on all entities                            | Add version field to Lead, QuotationGroup, Policy, Payment, Claim     | OPEN        |
| G029 | P2  | R11     | Claims         | Claims not linked to Policy eligibility gate             | Claim can be created without checking policy.status and incidentDate| Eligibility gate: policy ACTIVE/RENEWED AND incidentDate in coverage  | No eligibility check before SUBMITTED                                  | ClaimsEligibilityService.validate() before claim creation              | OPEN        |
| G030 | P3  | R4      | Motor          | Motor form uses POSP/NCB fields inconsistently            | NCB_OPTIONS inconsistency between PolicyFormPackage and motorFormConfig| All forms must use same NCB_OPTIONS from motorFormConfig (fixed: PolicyFormPackage) | Different files imported different NCB formats                | Fixed in previous remediation; verify in R4 full regression            | CLOSED      |
| G031 | P3  | R12     | Admin          | Admin pages (audit, delivery-monitor, workflows) use MOCK_ arrays| MOCK_AUDIT_LOGS, MOCK_DELIVERY_LOGS, MOCK_GRAPH_NODES in admin pages| All admin data from API                                                | Admin pages built as UI mockups without backend integration            | Build admin API endpoints; replace MOCK_ arrays                        | OPEN        |
| G032 | P3  | R1      | Auth           | MinIO initialization fails at startup (falls back to local) | [Nest] WARN MinIO initialization failed. Falling back to local storage | MinIO configured and connected for dev environment                  | MinIO not configured in dev .env                                       | Configure MinIO credentials or document local-storage dev config       | OPEN        |

---

## GAP REGISTER LEGEND

Status:
  OPEN        → Not yet started
  IN_CONTRACT → Captured in binding contract; implementation not started
  IN_PROGRESS → Actively being implemented
  PARTIAL     → Partially addressed; regression test needed
  BLOCKED_xxx → Blocked by another gap (referenced)
  CLOSED      → Implemented + tested + gate passed

## GAP COUNTS

P0 gaps: 15 (all release blockers)
P1 gaps: 8
P2 gaps: 6
P3 gaps: 3

Total: 32 gaps identified in R0 forensic baseline.
